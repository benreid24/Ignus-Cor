#include "Shared/Entities/EntityConversation.hpp"
using namespace std;

EntityConversation::EntityConversation(Entity* o) : owner(o), currentNode(nullptr) {}

void EntityConversation::update() {
    //
}
